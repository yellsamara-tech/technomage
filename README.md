# Telegram Bot (FastAPI + aiogram 3) — Render Webhook Deployment

## 🚀 Быстрый старт

1. Создай `.env` по шаблону `.env.example`
2. Установи зависимости:
   ```bash
   pip install -r requirements.txt
